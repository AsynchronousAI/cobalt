//- Loads `luacov.runner` and immediately starts it.
// Useful for launching scripts from the command-line. Returns the `luacov.runner` module.
// @class module
// @name luacov
// @usage lua -lluacov sometest.lua
var runner = require("cov.src.runner");
runner.init();
return runner;
